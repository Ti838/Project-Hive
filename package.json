{
  "name": "projecthive",
  "version": "1.0.0",
  "description": "ProjectHive — Complete Full-Stack Student Team Collaboration & Incubation Platform",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node -e \"const fs = require('fs'); fs.cpSync('frontend/.next', '.next', {recursive: true}); fs.cpSync('frontend/.next', 'public', {recursive: true});\"",
    "start": "npm --prefix frontend run start",
    "server": "npm --prefix server run dev"
  },
  "dependencies": {
    "next": "^16.3.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}

