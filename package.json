{
  "name": "projecthive",
  "version": "1.0.0",
  "description": "ProjectHive — Complete Full-Stack Student Team Collaboration & Incubation Platform",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build && cd .. && node -e \"const fs = require('fs'); fs.cpSync('frontend/.next', '.next', {recursive: true}); fs.cpSync('frontend/public', 'public', {recursive: true});\"",
    "start": "cd frontend && npm run start"
  },
  "dependencies": {
    "next": "16.3.3",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  }
}
